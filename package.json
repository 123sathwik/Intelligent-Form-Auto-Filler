{
  "name": "intelligent-form-auto-filler",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Scaffolding for AI Form Auto-Filler",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --prefix frontend && pip install -r requirements.txt",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && uvicorn app.main:app --reload",
    "format:frontend": "npm run format --prefix frontend",
    "format:backend": "black backend/app && isort backend/app",
    "format": "npm run format:frontend && npm run format:backend",
    "lint:frontend": "npm run lint --prefix frontend",
    "lint:backend": "flake8 backend/app --max-line-length=100",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  }
}
